import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'spicy_sam-Admin',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
